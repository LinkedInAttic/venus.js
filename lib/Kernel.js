'use strict';

// var Settings = require('./Settings');

module.exports = Kernel;

/**
 * Venus' core object which orchestrates all other activity.
 * @constructor
 * @param {object} options - key/value hash.
 */
function Kernel(options) {
  console.dir(options);
}

/**
 * Run the application
 * @method
 */
Kernel.prototype.run = function () {

};

