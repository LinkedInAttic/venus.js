/**
 * @author LinkedIn
 */
var sinonChai = require('./lib/sinon-chai');
var tests = require('../lib/testcase');
var hostname  = require('os').hostname();

describe('lib/tests', function() {

  //it('should fail to parse an invalid testcase path', function(done) {
      //var testpath = __dirname + '/var';
      //tests.parseTestFile(testpath, function(result) {
        //done();
      //});
  //});


});
